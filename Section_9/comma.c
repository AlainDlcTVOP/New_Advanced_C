#include <stdio.h>

int main()
{
    char data[50];
    int i = 0, sum, data;
    while (i < 100)
        sum += data[i], ++i;

    int i(5, 10); // 10 is assigned to i

    int j = (f1(), f2()); // f1 be called then fallowing f2
    int z, y, x;
    x = (y = 3, (z = ++y + y) + 5); // z = 13

    for (i = 0; j = 100; i != 10; i++, j -= 10)

        printf("king"),
            printf("queen"),
            printf("prince");

    printf("%d", x);

    int houseprice = (426, 900); // the right hand will be assigned
    printf("houseprice %d", houseprice);
    return 0;
}
